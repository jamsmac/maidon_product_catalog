# Example environment variables for MYDON (Client-side VITE_* keys are PUBLIC)
# Copy this file to ".env" and fill in values locally. Do NOT commit real keys.

# Public endpoints (non-sensitive)
VITE_SUPABASE_URL=https://your-supabase-project.supabase.co

# Public keys (non-sensitive identifiers only)
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Third-party public IDs (safe to expose)
VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
VITE_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXX
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXX

# Development aids (optional)
VITE_GA_DEBUG=false

# WARNING: Never put secrets in VITE_* variables. Use server-side env for secrets.
# The entries below are intentionally blank to avoid accidental exposure.
VITE_OPENAI_API_KEY=
VITE_GEMINI_API_KEY=
VITE_ANTHROPIC_API_KEY=
VITE_PERPLEXITY_API_KEY=
